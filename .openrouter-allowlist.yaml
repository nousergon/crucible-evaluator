# .openrouter-allowlist.yaml — pre-cleared OpenRouter-literal matches
# (alpha-engine-config-I7864, wiring the fleet-wide guard). Schema:
# nousergon-lib scripts/openrouter_guard.py::load_allowlist.
schema_version: 1
entries:
  - path: director/agent.py
    pattern: base_url
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: director/agent.py
    pattern: env_key
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: director/agent.py
    pattern: provider_literal
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: director/retro.py
    pattern: base_url
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: director/retro.py
    pattern: env_key
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: director/retro.py
    pattern: provider_literal
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: infrastructure/deploy.sh
    pattern: base_url
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: tests/test_deploy_sh_authority.py
    pattern: base_url
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: tests/test_director.py
    pattern: base_url
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: tests/test_director.py
    pattern: env_key
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
  - path: tests/test_director.py
    pattern: provider_literal
    reason: >-
      Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment, docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
    expires: "2026-11-18"
    tracking: alpha-engine-config-I7864
