# Config / storage
boto3~=1.43
pydantic~=2.13
pyyaml~=6.0

# setuptools floor for PYSEC-2026-3447 (newly disclosed; CI's pip-audit gate
# started failing 2026-07-14 against the default 79.0.1 bundled by the
# GH Actions Python 3.11 image). Not a build-backend requirement here — direct
# pin exists solely to satisfy the security-audit gate.
setuptools>=84.0.0

# Numerics for grading / report-card aggregation
numpy~=2.5
pandas~=3.0

# Shared contracts: MetricRecord (alpha_engine_lib.metrics), statistical
# intervals (alpha_engine_lib.quant.stats.intervals), quant stats (sharpe /
# sortino / max_drawdown / PSR / DSR / CVaR / BH-FDR), agent schemas, secrets.
# v0.52.0 — Phase C consumes MetricRecord + derive_* + the quant battery.
# v0.4.0 — director/emailer.py consumes krepis.console.console_url (config#1300).
# Bumped to >=0.6.0 (config#1450, same fix as crucible-executor#315 /
# crucible-predictor#320/#322): krepis>=0.6.0 pins flow-doctor[diagnosis,s3]
# >=0.6.2, whose DEFAULT_DIAGNOSIS_MODEL is the served `claude-sonnet-4-6`
# alias. The old >=0.4.0 floor let krepis 0.4/0.5 resolve flow-doctor
# 0.6.0rc6, whose retired `claude-sonnet-4-6-20250514` pin 404'd on every
# flow-doctor diagnosis call from grading/handler.py and director/handler.py.
# Bumped to >=0.7.0 (config#1494): deploy.sh's two canaries now CALL the
# `krepis.aws invoke-canary` CLI (0.7.0 ships it) instead of a bare
# `aws lambda invoke`. The invoke is now LOAD-BEARING, so krepis must be
# importable in the deploy environment — this floor makes it a hard dep.
# Bumped to >=0.9.0 (config#1673): director/retro.py's Phase-G retro judge
# now calls `krepis.llm` (the provider-agnostic LLMClient/ModelSpec adapter),
# introduced in krepis 0.9.0 — LOAD-BEARING for the retro's judge call
# (judge != generator; the plan-generation call in director/agent.py is
# untouched and stays on langchain-anthropic below).
# config#1694: notify_on_category required flow-doctor >=0.8; krepis flow_doctor
# extra capped <0.7 until v0.10.3 (krepis#14). Direct pin removed once floor
# is >=0.10.3 — the extra alone resolves flow-doctor 0.8.x.
# config#6050 (2026-08-01): the [openai] extra is LOAD-BEARING, not optional.
# director/agent.py migrated direct-Anthropic -> OpenRouter on 2026-07-24
# (DIRECTOR_MODEL='glm-5.2', routed through krepis.llm.LLMClient), and
# krepis's own pyproject states: `krepis[openai]` for OpenAI / OpenRouter /
# any OpenAI-compatible endpoint. The extra was never added, so every
# Director invocation since has died at import with
# `ModuleNotFoundError: No module named 'openai'` — surfaced live on
# watch-rerun-2026-08-01-4 (SF event 858, director/agent.py:182).
# Declared via the krepis extra rather than a bare `openai>=1.40` line so
# the version stays krepis's to own — the same single-source rule the
# nousergon-lib layer above learned the hard way (2026-07-11 drift).
#
# 2026-08-02: floor raised from >=0.24.1 to >=0.26.0 — 0.26.0 adds
# AppConfig-based registry resolution (resolve_group_structured can now
# read the LLM_MODEL_REGISTRY from AppConfig when KREPIS_APPCONFIG_APPLICATION
# is set), which is load-bearing for this Lambda (public repo, no
# private-docs/ on disk). The Director now addresses the "ultra" GROUP
# rather than pinning a model id.
#
# 2026-08-02: floor raised to >=0.27.0 — LOAD-BEARING, not a courtesy bump.
# 0.27.0 adds the `exec_context` and `wire` parameters to
# resolve_group_structured (model-router-policy R28/R29). Against 0.26.x both
# are unexpected keyword arguments and director/agent.py::_default_llm raises
# TypeError at the first Director call of the week — which is a LOUD failure
# and the correct one, but the floor is what keeps it from being installed at
# all. Below this version the Director cannot express which execution context
# it runs in, and the resolver falls back to serving openrouter.ai direct
# (alpha-engine-config-I6183).
#
# 2026-08-03: raised again to >=0.30.0, and this floor is the ONLY thing that
# makes the Director actually route through the proxy. Three defects, each of
# which independently sent the weekly `ultra` call to openrouter.ai
# DLP-unscanned while logging a healthy route:
#
#   0.29.0  an undeclared `reachable_from` was read as reachable from EVERY
#           context, so a stale registry copy silently re-enabled the direct
#           route; and `exclude_route` — the argument that let this Lambda skip
#           the proxy entirely — is gone rather than deprecated.
#   0.29.0  the master-key SSM lookup shelled out to `aws --profile
#           ne-laptop-daemon`, which cannot work in a Lambda for three separate
#           reasons, all swallowed. It now uses boto3.
#   0.30.0  the health probe was `HTTPConnection` unconditionally, so against
#           the router's TLS edge (router.nousergon.ai:8443, added
#           alpha-engine-config-I6194) the handshake failed and the proxy was
#           reported "not reachable" — at the same minute that URL was serving
#           23 models. Resolution then fell through to openrouter.
#
# Below 0.30.0 the Director resolves openrouter no matter what the registry,
# the edge or the credential say. A floor, not a pin, because krepis is ours
# and forward versions are the tested path.
#
# 2026-08-04: raised to >=0.31.1. `resolve_group_spec` (0.31.0) returns a ready
# ModelSpec instead of leaving the consumer to build one, and 0.31.1 is what
# maps the proxy route to a plain OpenAI transport against the edge URL. Below
# it, `provider: "litellm"` binds to TRANSPORT_LITELLM — an IN-PROCESS LiteLLM
# Router that calls each provider directly from this Lambda, reading
# OPENROUTER_API_KEY from the environment. That is unscanned egress bypassing
# the authenticated edge entirely, and it surfaced only on the first REAL
# invoke as `ModuleNotFoundError: No module named 'litellm'` — a dry run stops
# before .invoke() and cannot reach it.
# v0.54.0 (krepis#137, alpha-engine-config#6140, 2026-08-12): console_url's
# DEFAULT_CONSOLE_BASE_URL moved console.nousergon.ai -> dashboard.nousergon.ai.
# The console.nousergon.ai hostname was handed to nousergon-console v2; the
# Streamlit dashboard the Director digest deep-links into lives at
# dashboard.nousergon.ai. LOAD-BEARING for the emailed link, and
# tests/test_director_emailer.py now pins the new host — a resolve below
# 0.54.0 would build a link to the wrong service and the test would say so.
#
# 2026-08-13 (alpha-engine-config-I7179): krepis-PR140 (>=0.57.0) makes
# `LLMClient` resolve a default `cost_sink` from KREPIS_COST_SINK_BUCKET /
# KREPIS_COST_SINK_PREFIX when the call site doesn't pass one —
# director/agent.py's `_default_llm` never has, so the Director's
# `ultra`-group plan call (the pipeline's single most expensive LLM call)
# attributed its spend to nothing. It also ships `krepis.aws
# merge-lambda-env`, the read-modify-write CLI infrastructure/deploy.sh now
# uses to land the two env vars on the Director Lambda without clobbering
# the operator-set vars a bare `--environment` write would wipe. Subsumed by
# the higher floor below; kept because the floor's REASON is not recoverable
# from the version number.
# v0.59.2 (krepis#148, alpha-engine-config-I7334/-I7357): the FIRST release
# shipping krepis.stage_coverage — the module
# `grading/handler.py::_record_stage_coverage` imports. It was briefly added to
# nousergon-lib (nousergon-lib#314) and immediately removed as a duplicate of
# the krepis landing (nousergon-lib#315) — it never existed on any released
# nousergon-lib version, only here. Below this floor, every
# `_record_stage_coverage` call takes the ImportError branch and the I7214
# assertion never executes (measured live 2026-08-14, alpha-engine-config-I7334).
#
# The floor was `>=0.59.1` as merged in #209 and that was WRONG — measured by
# walking the tags, `src/krepis/stage_coverage.py` is ABSENT from v0.59.0 and
# v0.59.1 and first present at v0.59.2:
#
#   $ git ls-tree -r v0.59.1 --name-only | grep -c stage_coverage   -> 0
#   $ git ls-tree -r v0.59.2 --name-only | grep -c stage_coverage   -> 2
#
# 0.59.1 is a real, non-yanked release, so the old floor could resolve to a
# version lacking the module and silently reproduce the exact ImportError
# condition #209 exists to close. A floor one patch too low is indistinguishable
# from a correct one until the resolver happens to pick the gap.
krepis[flow-doctor,openai]>=0.59.2
# config#2454: grading/aggregate.py imports
# nousergon_lib.quant.stats.trial_accumulator (the cumulative DSR trial-count
# reader) ships in v0.124.3 (lib#198, merged 2026-07-17).
nousergon-lib[quant-stats,contracts] @ git+https://github.com/nousergon/nousergon-lib@v0.124.3

# Director (Layer C) — single structured LLM call for PLAN GENERATION
# (director/agent.py, DIRECTOR_GROUP="ultra", resolved through
# krepis.router.resolve_group_structured). langchain-anthropic provides
# ChatAnthropic + with_structured_output; only needed when
# DIRECTOR_ENABLED is on (imported lazily, so the test suite + the grading
# path don't require it). The Phase-G retro JUDGE call (director/retro.py)
# is a separate model tier (high group) routed through krepis.llm instead —
# see the krepis>=0.9.0 bump above.
langchain-anthropic~=1.5
langchain-anthropic~=1.5

# Dev / testing
pytest~=9.1
pytest-cov~=7.1
# moto: mocked S3 for artifact-read integration tests (added with the grading layer)
moto~=5.2
